# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CustomPopOver' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CustomPopOver
  pod 'Popover', '~> 1.2'

  target 'CustomPopOverTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CustomPopOverUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
